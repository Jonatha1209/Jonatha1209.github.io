---
title: [알고리즘 강좌 - 매우 큰 수를 거듭제곱 하기] 
description: 문제 풀이
author: jonathan
date: 2025-03-21
categories: [알고리즘 강좌]
tags: [백준, 알고리즘]
---

## 서론

오늘 알아볼 알고리즘은 분할정복을 이용한 거듭제곱 입니다.

우리가 거듭 제곱 할때는 보통 엄청 작은 수는 빠르게, 조금 큰 수는 살짝 빠르게, 큰 수는 느리게라고 표현합니다.

그렇기 때문에, 이런 큰 수 거듭 제곱 문제에서는 대부분 분할 정복을 이용한 거듭제곱을 사용한다 합니다.

근데, 분할 정복을 이용한 거듭 제곱이 뭘까요?

오늘 알아볼 알고리즘. 분할 정복을 이용한 거듭제곱입니다.

사실 알고리즘이라 말하긴 민망하긴 한데 ㅎㅎ 알아보도록 하죠

## 본론

대충 다음과 같은 문제가 있다고 쳐봅시다.

```text
자연수 A를 B번 곱한 수를 알고 싶다. 단 구하려는 수가 매우 커질 수 있으므로 이를 C로 나눈 나머지를 구하는 프로그램을 작성하시오.
```

다만 다음과 같은 제한이 있습니다.

```text
A, B, C는 모두 2,147,483,647 이하의 자연수이다.
```


일반적인 pow 코드를 내면 당연히 문제에서 시간초과가 나게 될겁니다.

예시로 틀린 코드의 소스코드를 링크로 달아놓겠습니다.

[틀린코드](https://www.acmicpc.net/source/share/187f5aa99aa04649a71a6b40c5040423)

그렇기에, 시간제한을 해소하기 위한 분할 정복 + 재귀를 사용해서 해소해줄겁니다.

대충 수 ```
A```를 ``` 
N``` 번 곱한다 쳐보겠습니다. 

이떄 걸리는 시간복잡도는 O(n)입니다.

반면, 분할 정복을 사용한 거듭제곱 계산법은 O(log n)의 시간 복잡도를 가집니다. 

예를 들어, (2^{32})를 계산한다고 할 때, 직접 계산 방식은 32번의 곱셈을 필요로 하는 반면, 분할 정복 방식은 단 5번의 곱셈만을 필요로 합니다. 이는 계산 과정에서 필요한 시간을 대폭 줄여줍니다.

거듭제곱 분할정복은 반복문과 재귀문이 있는데..

음...

솔직히 차이 모르겠습니다. 

편한거 쓰세요

### 반복문

```python
def pow(a, n):
    res = 1
    while n > 0:
        if n % 2 == 1:
            res *= a
            n -= 1
        a *= a
        n //= 2
    return res
```

### 재귀문

```python
def pow(a, n):
    if n == 0:
        return 1
    elif n % 2 == 0:
        half = pow(a, n // 2)
        return half * half
    else:
        half = pow(a, (n-1) // 2)
        return half * half * a
```

파이썬에선 기본 함수인 pow 함수를 써도 된다만, C++로 푸신다면 분정 거듭제곱 사용하는걸 추천드립니다.

또한, 나중에 갈수록 제한이 미친 문제들이 엄청 많으니, 분정 거듭제곱을 적극 사용 추천드립니다.

## 연습으로 풀 만한 문제

### BOJ 1629

평범한 분정 거듭제곱 문제입니다.
다만 코드를 약간 수정해줘야합니다.

재귀 코드와 반복문 코드를 각각 따로 올려놨습니다.

[반복문](https://www.acmicpc.net/source/share/dbf6cdb70470431890f8c015635c564b)

[재귀문](https://www.acmicpc.net/source/share/893de96d135e4e08a9fb6c6748dd4edd)

### 그외에 풀만한 연습 문제들

#### [BOJ 13171](https://www.acmicpc.net/problem/13171)

재귀문 또는 반복문 코드에서 mod부분을 다르게 처리해주면 AC 가능합니다.

#### [BOJ 13172](https://www.acmicpc.net/problem/13172)

문제를 열심히 읽다보면 어쨌든 b의 모듈러 곱셈에 대한 역원 (b^-1)을 구해야 한다는 것을 깨달을 수 있는데, 그 값은 (b^x-2)%x 를 통해 얻을 수 있다는걸 알게됩니다.
그걸 캐치하면 벌써 99%는 온겁니다. 나머진 뭐,, 구현하십쇼

#### [BOJ 15824](https://www.acmicpc.net/problem/15824)

분할 정복을 이용한 거듭제곱 GOAT 문제입니다.

처음 풀땐, 이상한 DP 문제인줄 알았는데 까고보니 분정 거듭제곱 이였던 문제네요.

어차피 최댓값을 더하고 최솟값을 빼면 되니 입력 배열 arr을 정렬을 한 뒤

i라는 기준을 잡고 i가 최댓값일 때 경우의 수는 더해주고 최솟값일 때 경우의 수는 빼주면 됩니다.

i가 최댓값일 경우는 1~i-1의 스코빌 지수는 정렬을 했으니 무조건 작거나 같기 때문에

1~i-1의 각각 인덱스가 포함될지 제외될지를 정하면 2^(i-1)이라는 경우의 수를 얻을 수 있습니다.

i가 최솟값일 경우는 i+1~n의 스코빌 지수는 무조건 크거나 같기 때문에 앞에처럼 구하면 2^(n-i)이라는 경우의 수를 얻을 수 있습니다.

최댓값 * 최댓값일 때 경우의 수 - 최솟값 * 최솟값일 때 경우의 수 = 2^(i-1) * arr[i] - 2^(n-i) * arr[i] = (2^(i-1) - 2^(n-i)) * arr[i]입니다.

더 자세한건 [여기](https://kimcodingvv.github.io/BOJ-15824/)를 참고하시는게 더 나을듯 합니다.